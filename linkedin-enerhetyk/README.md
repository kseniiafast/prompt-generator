# ECOFACTOR × ЕНЕРГЕТИК — LinkedIn concept (1080×1350)

Open `index.html` in a browser to preview. Export the `#canvas` element at 1080×1350.

## Drop-in assets (put in `assets/`)
1. `stations.jpg` — the EV-station photo (becomes the background automatically).
2. Official **ЕНЕРГЕТИК** logo — replace the `.enerhetyk` stand-in block in `index.html`.
3. Official **Global Compact Network Ukraine** logo — replace the `.gcnu` stand-in block.

`ecofactor-enerhetyk-linkedin.png` is a rendered preview (with placeholder background).
Built with the ECOFACTOR brand kit: Ruberoid type, green #3AF185, official ECOFACTOR logo.
